server {
    listen 80;
    server_name devbyanirban.com www.devbyanirban.com;

    return 301 https://devbyanirban.com$request_uri;
}

server {
    listen 443 ssl;
    server_name devbyanirban.com;

    ssl_certificate /etc/letsencrypt/live/devbyanirban.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devbyanirban.com/privkey.pem;

    root /var/www/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}